# Public origin used in canonical URLs, social previews, JSON-LD, and the sitemap.
NEXT_PUBLIC_SITE_URL=https://design.zeyadomran.com

# Leave empty for the recommended subdomain; set for subdirectory hosting only.
NEXT_PUBLIC_BASE_PATH=
